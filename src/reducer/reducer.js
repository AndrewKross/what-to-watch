const initialState = {
  genre: `all`,
};

const reducer = (state = initialState, action) => {
  switch (action) {
    default:
      return state;
  }
};

// eslint-disable-next-line import/prefer-default-export
export { reducer };
